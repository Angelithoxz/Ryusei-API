<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Stellar API</title>
    <link rel="stylesheet" href="/public/css/code.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <link rel="shortcut icon" href="/src/imagen/icon.png">
    <meta property="og:title" content="Home - Stellar API" />
    <meta name="description" content="Stellar API" />
    <meta property="og:image" content="/src/imagen/icon.png">
    
    <!-- Ocultar admin por defecto -->
    <style>
        #adminSection {
            display: none;
        }
    </style>
</head>
<body>
    <header class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">
                <i data-lucide="bar-chart-3"></i>
                <span>Stellar API</span>
            </a>
            <button class="nav-hamburger" id="navHamburger">
                <i data-lucide="menu"></i>
            </button>
        </div>
    </header>

    <div class="drawer-menu" id="drawerMenu">
        <div class="drawer-links">
            <a href="/api" class="drawer-link"><i data-lucide="file-code-2"></i> Endpoints</a>
            <a href="/logout" class="drawer-link"><i data-lucide="log-out"></i> Logout</a>
            <a href="/profile" class="drawer-link"><i data-lucide="user"></i> Profile</a>
            <a href="/about" class="drawer-link"><i data-lucide="user-circle"></i> About</a>
           <a href="/store" class="drawer-link"><i data-lucide="shopping-bag"></i> Shop</a>
            <div id="adminSection">
                <a href="/admin" class="drawer-link"><i data-lucide="user-cog"></i> Admin</a>
            </div>
        </div>
    </div>

    <main>
        <section id="home" class="main-section">
            <div class="section-container">
                <div class="section-header">
                    <h2>Server Statistics</h2>
                </div>
                <div class="stats-grid" id="stats-grid">
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="globe"></i>
                            <span>Your IP Address</span>
                        </div>
                        <div class="stat-card-value" id="user-ip">Loading...</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="mouse-pointer-click"></i>
                            <span>Total Requests</span>
                        </div>
                        <div class="stat-card-value" id="total-requests">Loading...</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="users"></i>
                            <span>Unique Visitors</span>
                        </div>
                        <div class="stat-card-value" id="unique-visitors">Loading...</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="cpu"></i>
                            <span>CPU Model</span>
                        </div>
                        <div class="stat-card-value small-text" id="cpu-model">Loading...</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="memory-stick"></i>
                            <span>Memory Usage</span>
                        </div>
                        <div class="stat-card-value" id="memory-usage">Loading...</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="server"></i>
                            <span>Operating System</span>
                        </div>
                        <div class="stat-card-value small-text" id="os-info">Loading...</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-card-header">
                            <i data-lucide="timer"></i>
                            <span>Server Uptime</span>
                        </div>
                        <div class="stat-card-value" id="server-uptime">Loading...</div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- jQuery necesario para el script -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/public/js/code.js"></script>
    <script>
    // Mostrar el enlace de admin si el usuario es administrador
    $(document).ready(function() {
        $.get('/api/user/data', function(data) {
            if (data.admin) {
                $('#adminSection').show();
            }
        }).fail(function() {
            console.error("Error al cargar datos del usuario.");
        });
    });
    </script>
</body>
</html>